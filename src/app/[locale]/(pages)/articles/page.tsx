import React from 'react';
import Moderation from "@/app/[locale]/components/shared/messagesClient/moderation";

const Page = () => {
    return (
        <div>
            <Moderation/>
        </div>
    );
};

export default Page;