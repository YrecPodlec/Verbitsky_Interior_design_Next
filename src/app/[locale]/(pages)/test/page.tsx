import React from 'react';
import {LanguageModal} from "@/app/[locale]/components/widgets";

const Page = () => {
    return (
        <div>
            <LanguageModal/>
        </div>
    );
};

export default Page;