import React from 'react';
import {Contacts} from "@/app/[locale]/components/widgets";

const Page = () => {
    return (
        <div>
            <Contacts/>
        </div>
    );
};

export default Page;